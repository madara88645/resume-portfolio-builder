export const DATA_PARSING_PROMPT = `Parse the uploaded CV text or LinkedIn data into structured JSON: experience, education, skills, projects, and contact fields.`;

export const SUMMARY_PROMPT = `Using the structured data, write a 3–4 sentence professional summary in a job-application-ready tone.`;

export const PORTFOLIO_PROMPT = `Create a minimal HTML/CSS design for a single-page portfolio. Must be responsive, clean, and professional. Include placeholders for user data.`;

export const DOWNLOAD_INSTRUCTIONS = `Provide the final portfolio as a downloadable HTML file AND a shareable link.`;
